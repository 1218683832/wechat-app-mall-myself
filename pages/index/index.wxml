<!--index.wxml-->
<view class="container">
  <view class="userinfo">
    <!-- <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称  -->
    <!-- <button open-type="getUserInfo" bindgetuserinfo="onGetUserInfo"> 获取头像昵称  -->
    <button open-type="getUserInfo" bindtap="opens"> 获取头像昵称 
    </button>
    <!-- <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block> -->
  </view>

  <view class="userlogin">
    <button bindtap='bindtap_userlogin'>登陆</button>
  </view>
</view>
